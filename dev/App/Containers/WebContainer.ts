/// <reference path='../../Scripts/typings/chitu.d.ts' />

// var _createPageContainer = chitu.PageContainerFactory.createPageContainer;
// chitu.PageContainerFactory.createPageContainer = function(routeData: chitu.RouteData, prevous: chitu.Page) {
//     return _createPageContainer(routeData, prevous);
// }